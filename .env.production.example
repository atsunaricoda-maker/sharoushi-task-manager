# ===================================================
# 本番環境用環境変数テンプレート
# ===================================================
# 重要: このファイルをコピーして.env.productionを作成し、
#      実際の値を入力してください。
#      .env.productionは絶対にGitにコミットしないでください！
# ===================================================

# Google OAuth設定（本番用）
# Google Cloud Console (https://console.cloud.google.com/)で設定
GOOGLE_CLIENT_ID=your_production_google_client_id_here
GOOGLE_CLIENT_SECRET=your_production_google_client_secret_here

# JWT Secret（本番用に新しく生成すること）
# 生成コマンド: openssl rand -base64 32
JWT_SECRET=your_production_jwt_secret_here

# Gemini API Key（本番用）
# https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_production_gemini_api_key_here

# SendGrid API Key（メール通知用）
# https://sendgrid.com/ でアカウント作成後、APIキーを取得
SENDGRID_API_KEY=your_production_sendgrid_api_key_here

# SendGrid送信者設定
SENDGRID_FROM_EMAIL=noreply@yourdomain.com
SENDGRID_FROM_NAME=社労士事務所タスク管理システム

# アプリケーション設定（本番用）
APP_URL=https://your-project-name.pages.dev
REDIRECT_URI=https://your-project-name.pages.dev/auth/callback

# 環境識別子
ENVIRONMENT=production

# ===================================================
# Cloudflare Pages Secret設定方法
# ===================================================
# 以下のコマンドで各シークレットを設定:
#
# npx wrangler pages secret put JWT_SECRET --project-name your-project-name
# npx wrangler pages secret put GEMINI_API_KEY --project-name your-project-name
# npx wrangler pages secret put SENDGRID_API_KEY --project-name your-project-name
# npx wrangler pages secret put GOOGLE_CLIENT_ID --project-name your-project-name
# npx wrangler pages secret put GOOGLE_CLIENT_SECRET --project-name your-project-name
# npx wrangler pages secret put SENDGRID_FROM_EMAIL --project-name your-project-name
# npx wrangler pages secret put SENDGRID_FROM_NAME --project-name your-project-name
#
# ===================================================